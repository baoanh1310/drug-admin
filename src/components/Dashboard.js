import * as React from "react";
import { Card, CardContent, CardHeader } from '@mui/material';

const Dashboard = () => (
    <Card>
        <CardHeader title="VAIPE Drugs Administration" />
        <CardContent>VAIPE Drugs Manage Website</CardContent>
    </Card>
);

export default Dashboard;